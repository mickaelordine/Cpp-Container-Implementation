#pragma once

/**
 * \brief 
 * in this class i will implement the main feature introduced with varint in cpp17, which is the ability to hold a value of one of several types.
 * the implementation will be based on a union, which is a data structure that can hold a value of one of several types, but only one type at a time.
 */

template < typename... Types>
class Variant
{
public:
    
};
